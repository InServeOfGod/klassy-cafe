<form method="post" action="{{ path('app_chefs_slider_delete', {'id': chefs_slider.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chefs_slider.id) }}">
    <button class="btn">Delete</button>
</form>
