<form method="post" action="{{ path('app_chefs_slider_delete', {'id': chefs_slider.id}) }}" class="d-inline" data-toggle="tooltip" data-placement="top" title="Delete">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chefs_slider.id) }}">
    <button type="submit" class="text-danger border-0 bg-transparent">
        <i class="fa fa-trash"></i>
    </button>
</form>
