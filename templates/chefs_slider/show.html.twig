{% extends 'base.html.twig' %}

{% block title %}ChefsSlider{% endblock %}

{% block body %}
    <h1>ChefsSlider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chefs_slider.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ chefs_slider.title }}</td>
            </tr>
            <tr>
                <th>Subtitle</th>
                <td>{{ chefs_slider.subtitle }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ chefs_slider.photo }}</td>
            </tr>
            <tr>
                <th>Facebook</th>
                <td>{{ chefs_slider.facebook }}</td>
            </tr>
            <tr>
                <th>Twitter</th>
                <td>{{ chefs_slider.twitter }}</td>
            </tr>
            <tr>
                <th>Instagram</th>
                <td>{{ chefs_slider.instagram }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chefs_slider_index') }}">back to list</a>

    <a href="{{ path('app_chefs_slider_edit', {'id': chefs_slider.id}) }}">edit</a>

    {{ include('chefs_slider/_delete_form.html.twig') }}
{% endblock %}
