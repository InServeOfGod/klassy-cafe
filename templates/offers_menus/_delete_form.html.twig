<form method="post" action="{{ path('app_offers_menus_delete', {'id': offers_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offers_menu.id) }}">
    <button class="btn">Delete</button>
</form>
