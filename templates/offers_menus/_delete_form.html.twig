<form method="post" action="{{ path('app_offers_menus_delete', {'id': offers_menu.id}) }}" class="d-inline" data-toggle="tooltip" data-placement="top" title="Delete">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offers_menu.id) }}">
    <button type="submit" class="text-danger border-0 bg-transparent">
        <i class="fa fa-trash"></i>
    </button>
</form>
