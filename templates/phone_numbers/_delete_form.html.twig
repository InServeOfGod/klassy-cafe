<form method="post" action="{{ path('app_phone_numbers_delete', {'id': phone_number.id}) }}" class="d-inline" data-toggle="tooltip" data-placement="top" title="Delete">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone_number.id) }}">
    <button type="submit" class="text-danger border-0 bg-transparent">
        <i class="fa fa-trash"></i>
    </button>
</form>
