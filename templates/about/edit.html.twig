{% extends 'base.html.twig' %}

{% block title %} | {{ title }}{% endblock %}

{% block admin_body %}
    <section class="container">
        {{ form_start(form) }}
        <div class="card card-primary">
            <div class="card-header">
                <h3 class="card-title">{{ title }}</h3>
            </div>
            <!-- /.card-header -->
            <!-- form start -->

            <div class="card-body">
                <div class="form-group">
                    {{ form_row(form.title, {attr: {class: "form-control", placeholder: "Title"}}) }}
                </div>

                <div class="form-group">
                    {{ form_row(form.video_link, {attr: {class: "form-control", placeholder: "Video link"}}) }}
                </div>

                <div class="form-group">
                    <div class="custom-file">
                        {{ form_widget(form.video_bg, {attr: {class: 'custom-file-input', id: 'customFile'}}) }}
                        <small class="text-muted">
                            {{ form_help(form.video_bg) }}
                        </small>

                        <small class="text-danger">
                            {{ form_errors(form.video_bg) }}
                        </small>
                        <label class="custom-file-label" for="customFile">{{ about.videoBg ?? null }}</label>
                    </div>
                </div>

                {% if about.videoBg is not null %}
                    <img src="{{ asset(about.videoBg) }}" alt="{{ about.videoBg }}" width="64" height="64" class="img-rounded">
                {% endif %}
            </div>
            <!-- /.card-body -->

            <div class="card-footer">
                <button type="submit" class="btn btn-primary float-left">Update</button>
                <a href="{{ path('app_field_about_index') }}" class="float-right btn btn-secondary">
                    <i class="fas fa-angle-left"></i>
                    Back to List
                </a>
            </div>
        </div>
        {{ form_end(form) }}
    </section>
{% endblock %}

{% block js %}
    <script src="{{ asset('plugins/bs-custom-file-input/bs-custom-file-input.min.js') }}"></script>
    <script type="text/javascript">
        $(function () {
            bsCustomFileInput.init();
        });
    </script>
{% endblock %}
