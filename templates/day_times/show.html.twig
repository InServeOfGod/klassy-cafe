{% extends 'base.html.twig' %}

{% block title %} | {{ title }}{% endblock %}

{% block admin_body %}
    <div class="container">
        <div class="card card-primary mt-3">
            <div class="card-header">
                <h3 class="card-title">{{ title }}</h3>
            </div>
            <div class="card-body p-0">
                <table class="table table-hover">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ day_time.id }}</td>
                        </tr>
                        <tr>
                            <th>Day Time</th>
                            <td>{{ day_time.dayTime }}</td>
                        </tr>
                        <tr>
                            <th>Photo</th>
                            <td>
                                <a href="{{ asset(day_time.photo) }}" target="_blank">
                                    <img src="{{ asset(day_time.photo) }}" alt="{{ day_time.photo }}" width="64" height="64" class="img-thumbnail">
                                </a>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="card-footer">
                <a href="{{ path('app_day_times_index') }}" class="float-left btn btn-secondary">
                    <i class="fas fa-angle-left"></i>
                    Back to List
                </a>

                <div class="float-right">
                    <a href="{{ path('app_day_times_edit', {'id': day_time.id}) }}" class="text-secondary" data-toggle="tooltip" data-placement="top" title="edit">
                        <i class="fa fa-edit"></i>
                    </a>
                    {{ include('day_times/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
